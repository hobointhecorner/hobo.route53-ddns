#!/bin/bash

cat {{ ddns_config_dir|default('/srv/route53-ddns')}}/.registry_token | docker login {{ ddns_container_registry|default('ghcr.io') }} -u {{ ddns_container_registry_username }} --password-stdin
